// NOTICE: This file is generated by Rollup. To modify it,
// please instead edit the ESM counterpart and rebuild with Rollup (npm run build).
'use strict';

const typeGuards = require('./typeGuards.cjs');

/**
 * @param {import('postcss').Node} node
 * @returns {boolean}
 */
function isInDocument(node) {
	let current = node;

	while (current) {
		if (typeGuards.isDocument(current)) return true;

		// Check if current node has a document property
		if (
			'document' in current &&
			current.document &&
			typeGuards.isDocument(/** @type {import('postcss').Document} */ (current.document))
		)
			return true;

		// Only assign if parent exists
		if (!current.parent) break;

		current = current.parent;
	}

	return false;
}

exports.isInDocument = isInDocument;
