name: Release

on:
  pull_request:
    branches: [main]
    types: [closed]

jobs:
  release:
    uses: stylelint/.github/.github/workflows/call-release.yml@61e84671c2d4d7af47353e7f5289a2df81bd681d # 0.7.0
    permissions:
      contents: write
      id-token: write
