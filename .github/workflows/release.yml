name: Release

on:
  pull_request:
    branches: [main]
    types: [closed]

jobs:
  release:
    uses: stylelint/.github/.github/workflows/call-release.yml@de22fededfbe64a953912f8306e71923d7a503d3 # 1.0.1
    permissions:
      contents: write
      id-token: write
